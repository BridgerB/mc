// Personal OCI configuration
// Copy this file to config.ts and fill in your own values

export const config = {
  // OCI Tenancy OCID
  // Find this in: OCI Console → Profile → Tenancy: <name> → OCID
  tenancyId: "ocid1.tenancy.oc1..aaaaaaaaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",

  // NixOS ARM image OCID (for your region)
  // Find this in: OCI Console → Compute → Custom Images
  // Or use official NixOS ARM images from https://nixos.org/download.html#nixos-oci
  imageId:
    "ocid1.image.oc1.phx.aaaaaaaaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",

  // Subnet OCID (for your region and VCN)
  // Find this in: OCI Console → Networking → Virtual Cloud Networks → Your VCN → Subnets
  subnetId:
    "ocid1.subnet.oc1.phx.aaaaaaaaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",

  // SSH public keys for root user access
  // Add your SSH public keys here (from ~/.ssh/id_ed25519.pub or similar)
  sshKeys: [
    "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... your-username@hostname",
    // You can add multiple keys
  ],
} as const;
